name: Test

on:
  push:
  pull_request:
  release:

jobs:
  test:

    runs-on: ubuntu-latest

    steps:
    - uses: denoland/setup-deno@v1
      with:
        deno-version: v1.x
  
    - name: Test
      run: deno test
